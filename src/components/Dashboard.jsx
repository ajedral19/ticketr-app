import { Fragment } from "react";

const Dashboard = ({}) => {
    return (
        <Fragment>
            <div className="panel">
                <h1>Hello from dashboard</h1>
            </div>
        </Fragment>
    );
};

export default Dashboard;
